# ==============================================================================
# BADEHEYAAT MONOREPO - ENVIRONMENT VARIABLES TEMPLATE
# ==============================================================================
# Copy this file to .env and fill in the values.
# IMPORTANT: Never commit your actual .env file to version control.
# ==============================================================================

# --- Database Configuration (PostgreSQL) ---
DATABASE_CLIENT=postgres
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_NAME=badeheyaat
DATABASE_USERNAME=strapi
DATABASE_PASSWORD=strapi_password

# --- Strapi Secrets ---
# Generate these using: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ADMIN_JWT_SECRET=your_admin_jwt_secret
API_TOKEN_SALT=your_api_token_salt
TRANSFER_TOKEN_SALT=your_transfer_token_salt
APP_KEYS=key1,key2,key3,key4
JWT_SECRET=your_jwt_secret
ENCRYPTION_KEY=your_encryption_key

# --- Strapi API Integration ---
# Internal URL for containers (Next.js SSR & Microservice)
STRAPI_API_URL=http://strapi:1337
# Public URL for the browser (Next.js Client-side)
NEXT_PUBLIC_STRAPI_API_URL=http://localhost:1337

# --- API Tokens ---
# Generate these in Strapi Admin -> Settings -> API Tokens

# READ-ONLY token for the frontend (Axiom & Category 'find' and 'findOne' permissions)
FRONTEND_STRAPI_TOKEN=your_frontend_read_token

# UPDATE-ONLY token for the microservice (Axiom 'update' permission)
MICROSERVICE_STRAPI_TOKEN=your_microservice_update_token

# --- Analytics ---
NEXT_PUBLIC_GTM_ID=GTM-XXXXXXX

# --- CI/CD Deployment (GitHub Secrets) ---
# These are usually stored in GitHub Repository Secrets, not in the local .env
# But they are listed here for reference if you want to test locally.
# VPS_HOST=
# VPS_USERNAME=
# VPS_SSH_KEY=
# ENV_FILE_CONTENT=
